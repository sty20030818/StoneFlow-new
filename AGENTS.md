# StoneFlow Agent Instructions

## Agent skills

### Issue tracker

规格与实施 tickets 仅保存为仓库内的本地 Markdown，不发布到外部平台。详见 `docs/agents/issue-tracker.md`。

### Domain docs

StoneFlow 使用单一领域上下文，复用 `Documents/01-架构/` 下的既有领域与 ADR 文档。详见 `docs/agents/domain.md`。

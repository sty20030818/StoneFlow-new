# Filter · Display · Views · 列表查询 — 优化债台账

> **用途**：汇总「先前任务留下的过渡/兼容/双真源」与「本任务必须清掉的结构债」。  
> **原则**：禁止把过渡方案当长期；整洁架构 / 六边形：领域纯、端口清晰、单一真源。  
> **最后更新**：2026-08-03（P0–P6 后状态）

---

## 0. 怎么用

| 列 | 含义 |
|---|---|
| ID | 债编号 |
| 状态 | open / done / deferred |

归档前：本任务内 open 项须 done；后置项须有去向。

---

## 1. 总览（P6 时点）

| 层 | 健康度 | 说明 |
|---|---|---|
| Filter 产品模型 | 高 | FilterQuery + Menu/Bar + URL session；Command 非主路径 |
| Display | 高 | showCompleted / 设为默认 / 排序方向内嵌；与 View sort 分离 |
| View 持久化 | 高 | filters = clause；sort/group 产品停写 |
| list 查询衔接 | 中高 | effective→adapt；仍有扁平 controller 桥 |
| closeout 过渡结构 | 中 | serverDriven / externalFilter 待 P7 |
| View SQL 全量 | 中 | 仍内存窗口（F-02 后置） |

---

## 2. 本任务条目状态

### 2.1 双真源 / 边界

| ID | 长期终态 | 处置 | 状态 |
|---|---|---|---|
| D-01 | FilterQuery 唯一形状 | T1–T4, T19 | **done**（扁平 controller 仅桥，P7 删类型） |
| D-02 | View.filters = FilterQuery | T4 | **done** |
| D-03 | 呈现只 Display | T5–T6, T9 | **done** |
| D-04 | showCompleted ∈ Display | T7 | **done**（filter 状态仍有字段，P7 删） |
| D-05 | 锚定 Menu + Bar | T13–T14, T19 | **done** |
| D-06 | 临时 ∈ URL `f` | T2, T10 | **done** |

### 2.2 closeout 过渡（P7）

| ID | 处置 | 状态 |
|---|---|---|
| D-10 serverDriven 跳表 | P7 T24 | open |
| D-11 externalFilter 双挂 | P7 T25 | open |
| D-12 场景 adapt 一致 | 部分 done（list/project/views 已 adapt）；P7 复查 | open |
| D-13 useTaskViewRunQuery 旧路径 | P7 T27 | open |
| D-14 日期双枚举 | 部分（clause 统一）；P7 删 PageDateFilterValue | open |

### 2.3 架构味道

| ID | 状态 |
|---|---|
| D-20 filter/core 领域核 | **done** |
| D-21 collection 上提 filter | 部分；P7 T25 | open |
| D-22 工具条槽位 | **done** |
| D-23 Display ⊥ Filter | **done**（文档已写） |
| D-24 View SQL/内存显式 | P7 T29 | open |

### 2.4 删除清单（P7 全量 grep）

- [x] `openCommand('filter-picker'` **作为主入口**（register 已停用）
- [x] 产品读 View.sort/group 驱动 Board（已停；迁移写 display）
- [x] 运行时双解析旧 filters 无写回（decode 迁 clause；写回新形状）
- [ ] `PageFilterState` 整类型删除
- [ ] 扁平 controller 驱动 UI 主路径
- [ ] `serverDrivenFilters` 跳过表
- [ ] `externalFilter` + EMPTY 占位
- [ ] `showCompleted` 在 filter hasActive

---

## 3. 后置（有去向）

| ID | 去向 |
|---|---|
| F-01 菜单键入搜索 | 后续 UX 小任务 |
| F-02 View 全 SQL keyset | 独立查询任务 |
| F-03 List/Board 真布局 | 产品有 Board 时 |
| F-04 Advanced/AI/协作字段 | 产品就绪后 |
| F-05 Operator 文案矩阵 | 体验抛光 |
| F-06 多用户 default | 协作阶段 |
| F-07 Display 像素抛光 | UI polish |

---

## 4. 完成记录

| 日期 | 记录 |
|---|---|
| 2026-08-03 | 初版债台账 |
| 2026-08-03 | P0–P6 后刷新状态；主路径 Filter/Display/View 终态落地；P7 专清过渡结构 |

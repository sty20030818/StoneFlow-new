# M-0 · 零行为 Delete 包

| 字段 | 内容 |
|------|------|
| **状态** | `done`（代码已合入工作区 · 2026-07-16 · `bun run check` 绿） |
| **对应 Gap** | Top #6 M-0 包 · #8 DATA-D3 · PLAT-D3 healthcheck · SCN 空壳 · SpaceLayout |
| **To-Be 锚点** | [出口 §4 M-0](../../03-To-Be/07-Phase-C出口与Gap映射.md) · [T5 空壳](../../03-To-Be/05-Feature模块化.md) · [T3 DATA-D3](../../03-To-Be/03-数据与状态生命周期.md) · [路线图 M-0](../重构切片路线图.md) |
| **依赖史诗** | 无（Phase D 第一刀） |
| **建议 PR** | **1 个**（全是删除/去死引用，宜原子） |
| **日期** | 2026-07-16 |

---

## 1. 目标

删除**确认无生产价值**的空目录、孤儿 feature、半死 drawer store、仅测试用的 `SpaceLayout`，以及 dialog 对死 store 的空调用。  
**用户可感知行为不变**（抽屉仍走 entity-detail URL；壳装配逻辑不改）。

## 2. 非目标

- 不拆 `ShellLayout`、不做 CommandBridge（→ M-2）  
- 不抽 `useTaskListScene`、不改三列表行为（→ M-3）  
- 不修 shared 反向依赖（→ M-1）  
- 不改 badges / 裸 invoke（→ 刀）  
- 不批量清 doctor 全部 unused-export（仅删**整文件/整模块**级死代码；export 级可留给后续 deslop）  
- 不删仍被 route 引用的 `inbox`/`all-tasks` 等 Page（仅删其**空** `api/`/`model/` 子目录）

## 3. 触碰路径（执行前再 rg 复核）

### 3.1 整模块 / 整文件删除

| 路径 | 动作 | 依据（2026-07-16 扫描） |
|------|------|-------------------------|
| `src/features/task-drawer/` | 删整树（空 api/model） | 无实现；真抽屉在 task/detail + entity-detail |
| `src/features/healthcheck/` | 删整树 | 无 `src` 内外部 import；仅自测 |
| `src/app/layouts/SpaceLayout.tsx` | 删 | 生产零引用 |
| `src/app/layouts/SpaceLayout.test.tsx` | 删 | 仅测上述遗留入口 |
| `src/app/layouts/shell/model/useDrawerStore.ts` | 删 | 生产打开抽屉已走 `useEntityDetailController`；store 仅被 dialog 空 `closeDrawer` 与测试引用 |

### 3.2 连带修改（去死引用）

| 路径 | 动作 |
|------|------|
| `src/app/layouts/shell/model/useDialogStore.ts` | 去掉所有 `useDrawerStore.getState().closeDrawer()` 及 import（打开 dialog 时的互斥对**死 store** 已无效果；URL 抽屉互斥若需另开刀，**本刀不做**） |
| `src/app/layouts/shell/model/useDialogStore.test.ts` | 去掉 drawer store 准备/断言，只测 dialog |
| `src/features/lifecycle/ui/LifecycleList.test.tsx` | 去掉对 `useDrawerStore` 的 mock（若生产代码已不依赖） |
| `src/features/views/ui/ViewsPage.test.tsx` | 同上 |

### 3.3 空目录删除（保留 feature 有用文件）

| 路径 | 动作 |
|------|------|
| `src/features/inbox/api/` · `inbox/model/` | 删空目录 |
| `src/features/trash/api/` · `trash/model/` | 删空目录 |
| `src/routes/debug/` · `src/routes/spaces/`（若仍空且非 file-route 需要） | 删空目录；**删前确认**无 route 生成依赖 |

### 3.4 可选（时间允许再做；不做不挡 M-0 done）

| 路径 | 说明 |
|------|------|
| doctor `unused-file` 列表中**确认零引用**的 base 组件等 | 易误伤；默认 **跳过**，记入「M-0.1 deslop」 |

## 4. 步骤清单

### 4.0 开工确认

- [x] `rg` 与 §3 一致  
- [x] 执行  

### 4.1 空壳与孤儿 feature

- [x] 删除 `src/features/task-drawer/`  
- [x] 删除 `src/features/healthcheck/`  
- [x] 删除 `inbox`/`trash` 下空 `api`/`model`  

### 4.2 SpaceLayout

- [x] 删除 `SpaceLayout.tsx` + `SpaceLayout.test.tsx`  
- [x] `rg SpaceLayout src` → 零命中  

### 4.3 useDrawerStore

- [x] 改 `useDialogStore.ts`：移除 drawer close  
- [x] 更新 `useDialogStore.test.ts`  
- [x] LifecycleList / ViewsPage 改 mock `useEntityDetailController`  
- [x] 删除 `useDrawerStore.ts`  
- [x] `rg useDrawerStore src` → 零命中  

### 4.4 空 routes 目录

- [x] 删除空 `src/routes/debug`、`src/routes/spaces`  

### 4.5 验证

- [x] `bun run check` 绿（typecheck/lint/format/vitest/rust）  
- [ ] §6 手动冒烟（建议你本地跑一下 app）  

### 4.6 收尾

- [x] 状态 → `done`  
- [x] 路线图 M-0 标完成  
- [ ] PR 链接：（未提交则本地工作区）  

## 5. 风险与回滚

| 风险 | 缓解 | 回滚 |
|------|------|------|
| 某处仍 `openDrawer` 自 drawer store | 步骤 4.0 rg；生产打开已在 entity-detail | revert PR |
| 删 healthcheck 后某隐藏动态 import | 全仓 rg；无 barrel 再导出 | revert |
| 测试假依赖 drawer mock 失败 | 4.3 同步改测 | 修测或 revert |
| dialog 打开不关 URL 抽屉 | **As-Is 已然**（死 store）；本刀不扩大互斥语义 | 产品若要互斥 → 新刀：dialog → entityDetail.close |

## 6. 验收

### 6.1 自动化

```bash
bun run check
```

### 6.2 手动冒烟

| # | 路径 | 期望 |
|---|------|------|
| 1 | 启动 → 恢复进工作区 | 正常 |
| 2 | 列表点开任务 **抽屉**（URL `?task=` 或现网行为） | 仍打开/关闭正常 |
| 3 | 打开命令板 / 新建任务 dialog | 正常；无控制台报错 |
| 4 | 进入 archive/trash/settings | 正常 |
| 5 | Quick Create 窗（若方便） | 仍可用 |

### 6.3 doctor

- [x] 默认 **不跑**（可选：若跑，期望 unused-file 略降，不作为门禁）

## 7. 完成后

- [ ] 路线图 M-0 → done  
- [ ] 短契约：`layouts/ARCHITECTURE` 若仍写 SpaceLayout 生产入口 → **记入 Doc-C**，本刀可不改文档  
- [ ] PR：  
- [ ] 踩坑：  

---

## 8. 完成定义（DoD）

1. §3.1–3.3 目标路径已删或已改，rg 无死引用。  
2. `bun run check` 绿。  
3. 冒烟 §6.2 通过。  
4. **无** Shell 拆分、**无** 列表 DRY、**无** shared 迁文件。  

---

## 9. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-16 | 初稿 planned；路径按当日 rg |

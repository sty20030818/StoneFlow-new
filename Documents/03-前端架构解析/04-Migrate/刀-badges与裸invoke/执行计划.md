# 刀 · badges → Query + 设备偏好 api 收口

| 字段 | 内容 |
|------|------|
| **状态** | `done` |
| **对应 Gap** | **DATA-D2**（#9）· **DATA-D5**（#10） |
| **依赖** | β-acl（壳只依赖 feature public） |
| **风险** | 低 |

## 目标

1. `useSidebarNavBadges` 使用与列表相同的 **React Query keys**，去掉手写 list + 事件自刷双通道。
2. `shellDevicePreferences`（含 legacy invoke）迁入 **`features/settings/api`**，layout 只调 public。

## 非目标

- 不改 badge 产品文案/显示规则
- 不重做 sidebar 偏好同步协议

## 验收

- `bun run check` 绿
- layout 无 `invoke` 读设备偏好；badges 无 `listTasks` 直刷
- 角标随创建/删除任务变化（依赖既有 mutation invalidate）

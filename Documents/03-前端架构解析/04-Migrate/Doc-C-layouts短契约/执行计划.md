# Doc-C · layouts 短契约回写

| 字段 | 内容 |
|------|------|
| **状态** | `done`（正文已写入 `src/app/layouts/ARCHITECTURE.md` · 2026-07-16） |
| **对应 Gap** | DRIFT-004～006 · Top #13 |
| **To-Be 锚点** | T2/T4 生产路径 · M-0 已删 SpaceLayout |
| **依赖** | 无；与 M-0 互补（代码删了文档也要跟上） |

---

## 1. 目标

重写 `src/app/layouts/ARCHITECTURE.md`，与**当前代码事实**一致：

- 生产入口不是 `SpaceLayout`
- `rememberShellRoute` 在 **route / navigation**，不在 layout
- 无 `app/routing`
- 反映 M-0 删除项（SpaceLayout、useDrawerStore）
- 指向 To-Be（壳将上提 `src/layout`、拆分方向）但不假装已搬家

## 2. 非目标

- 不改 TS/TSX 业务代码  
- 不写完整 To-Be 目录树（细节在 Docs/03）  

## 3. 验收

- [x] 文档无 SpaceLayout 生产入口叙述  
- [x] 装配链与 As-Is W2 / 代码一致  
- [x] 依赖方向无 `app/routing`  
- [ ] （可选）人读一遍 Header/Sidebar 开发是否够用  

## 4. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-16 | 执行：重写 ARCHITECTURE.md |

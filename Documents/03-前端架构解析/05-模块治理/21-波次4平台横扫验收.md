# 波次 4 · Platform 横扫验收（2026-07-19）

> 状态：**验收完成** · 不做整包五步硬抄
> 总表：[12-平台与Domain扩散重构执行计划](./12-平台与Domain扩散重构执行计划.md)
> 口径：禁 feature→layout · 外模块只走 barrel · 不过度预防性 export · 无兼容层优先
> **进度/债只写本文**；各 feature `ARCHITECTURE` 仍为定稿日常（本轮未改定稿，除非后续短刀触及）。

---

## 0. 怎么判

| 结论 | 含义 |
|------|------|
| **过关** | 边界 + public 已干净，无需动 |
| **只验收通过** | 计划标「只验收」或旁注平台；无阻塞债 |
| **短刀** | 有限项可修（优先 public 收窄 / 相对路径）；**不开** SCENE/VOLUME 史诗 |

---

## 1. 计划内主档（按债短刀）

| 模块 | ~产线 | →layout | 结论 | 依据 |
|------|-------|---------|------|------|
| **selection** | ~0.8k | 0 | **过关** | public 精瘦且全有外消费者 |
| **display-options** | ~2.3k | 0 | **短刀** | 外仓 0 用：`isTaskDisplayPageKey` / `TASK_DISPLAY_STATIC_PAGE_KEYS` / `taskDisplayOptionsKeys` |
| **metadata-fields** | ~2.0k | 0 | **短刀** | public 类型面偏肥（多 type 外仓 0 hit） |
| **global-search** | ~0.7k | 0 | **短刀** | 多余 public：`GlobalSearchResults` / `useSearchEntitiesQuery` / `searchKeys` / `resolveTaskSearchTargetPath` 等 |

### 短刀清单（各 ≤3）

**display-options**
1. `index.ts` 撤外仓未用三符号
2. （可选）包内相对路径
3. （可选）`DisplayOptionsPanel` ~393 余力再拆

**metadata-fields**
1. 按外消费者收窄 public type
2. index 措辞对齐「唯一公共面」
3. （可选）低层工厂是否仍须外放

**global-search**
1. public 只留：`searchEntities` · `useGlobalSearch` · `useSearchFocusIntentStore` · `resolveProjectSearchTargetPath` · `GlobalSearchInput`（+必要 type）
2. Results / query hooks / 未用 resolve 收回包内
3. （可选）包内相对路径

---

## 2. 计划内只验收

| 模块 | ~产线 | →layout | 结论 |
|------|-------|---------|------|
| **submit** | ~0.4k | 0 | **只验收通过** |
| **danger-confirm** | ~0.3k | 0 | **只验收通过**（Dialog 外放偏宽可后置） |
| **filter** | ~0.2k | 0 | **只验收通过** |
| **workspace** | ~0.1k | 0 | **只验收通过** |

---

## 3. 旁注加行（同波次口径）

| 模块 | 结论 | 说明 |
|------|------|------|
| **entity-scene** | 只验收通过 | 槽位平台，纪律已在 |
| **entity-detail** | 只验收通过 | 控制器分层清晰 |
| **shell-dialogs** | 只验收通过 | 壳挂 store，边界对 |
| **sync** | 只验收通过 | 自深可后置，非主刀 |
| **bulk-action** | 无新债 | 维持波次 1b 轻扫 done |
| **update** | **短刀（旁注）** | ~1.8k；包内自深多；两文件 >300 — **勿 silently 只验收** |

**update 短刀（若排期）：** 相对路径 → 瘦 Settings/Dialog → 核 public。

---

## 4. 总判与建议执行序

1. **波次 4 验收结论成立**：无 feature→layout 系统性倒依赖；无 `@deprecated` 兼容层堆叠。
2. **短刀已落地（2026-07-19）**：display-options / metadata-fields / global-search 仅收窄 `index.ts` public；随后 §5.1 清死包装/未接线符号；门禁绿。
3. **旁注仍 pending**：`update`（相对路径 + 体量）另排；非阻塞收口。
4. 主档短刀勾完 → 波次 4 可标 **主线收口**；余 update 按债偶发。

---

## 5. 短刀落地记录

| 模块 | 结果 |
|------|------|
| display-options | 撤 `isTaskDisplayPageKey` · `TASK_DISPLAY_STATIC_PAGE_KEYS` · `taskDisplayOptionsKeys` |
| metadata-fields | 撤外仓 0 用 type（含 Props types）；去 `@fileoverview`；措辞对齐唯一公共面 |
| global-search | 只留 `searchEntities` · `useGlobalSearch` · `useSearchFocusIntentStore` · `resolveProjectSearchTargetPath` · `GlobalSearchInput` |

### 5.1 冗余/死代码续清（2026-07-19）

无 `@deprecated` 兼容层；清理的是可证死包装与未接线符号：

| 模块 | 删除/内联 |
|------|-----------|
| display-options | `resolveVisibleTaskDisplayProperties`（身份包装，内联为 `[...properties]`）及 `task-display-properties.ts` |
| metadata-fields | `createTaskStatus/PriorityMetadataOptions`；`createMetadataDateOptions`（只包 `Config`）；`stopMetadataFieldEventPropagation` 子 barrel/再导出 |
| global-search | `resolveTaskSearchTargetPath`（生产 Header 走回调，仅测在用）及对应测例 |

**未动（非死 / 可选债）：** space Options+Dropdown 双入口仍被 CreateDialogShell 用；包内 `@/features/...` 自深相对路径迁移；`update` 旁注短刀。

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-19 | 初版横扫；四主档 + 只验收 + 旁注 |
| 2026-07-19 | 三模块 public 短刀落地；波次 4 主线收口（update 旁注 pending） |
| 2026-07-19 | 三模块冗余/死代码续清（§5.1）；门禁绿 |

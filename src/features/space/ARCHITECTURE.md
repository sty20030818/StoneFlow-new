# space · 空间域

> 作用：描述 **当前已落地** 的 `src/features/space` 边界  
> 最后更新：2026-07-17

---

## 职责

- 可见 Space 列表、CRUD、默认 space、active scope IPC  
- 侧栏视觉 tokens（icon/color）  
- Space 编辑对话框  

**不负责：** 命令打开意图 pending（→ `@/features/command`）、任务/项目业务规则。

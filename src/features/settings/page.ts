/**
 * @fileoverview **settings/page · 页面入口（仅 routes）**
 *
 * 设置页 UI。壳 / navigation **不要** import 本入口，以免与 layout 形成环。
 *
 * ```ts
 * import { SettingsPage } from '@/features/settings/page'
 * ```
 */

export { SettingsPage } from './components/SettingsPage'
